""" 3.5.0_Python_KEY_WORDS """

# -----------

# Keywords in Python are reserved words that have special meaning.
#   They cannot be used as identifiers.

""" Python keywords 3.13 

    and – Logical operator that returns True if both operands are true.
    as – Creates an alias (used in imports or with blocks).
    assert – Checks a condition; raises an error if false (useful for debugging).
    async – Declares an asynchronous function or context.
    await – Waits for the result of an asynchronous function.
    break – Exits a loop early.
    class – Declares a class.
    continue – Skips to the next iteration of the loop.
    def – Declares a function.
    del – Deletes a variable or object.
    elif – Additional conditional branch (else if).
    else – Alternative conditional branch.
    except – Catches an exception in a try block.
    False – Boolean false value.
    finally – Block that always executes at the end of a try, whether an exception occurred or not.
    for – Starts a loop that iterates over a sequence.
    from – Imports specific parts of a module.
    global – Declares a variable as global within a function.
    if – Starts a conditional statement.
    import – Imports modules or libraries.
    in – Checks if a value is in a collection (list, tuple, string, etc.).
    is – Tests if two variables refer to the same object in memory.
    lambda – Creates an anonymous function.
    None – Represents the absence of a value.
    nonlocal – Declares a variable in an outer (but not global) scope.
    not – Logical negation operator.
    or – Logical operator that returns True if at least one operand is true.
    pass – Does nothing; used as a placeholder.
    raise – Raises an exception manually.
    return – Exits a function and returns a value.
    True – Boolean true value.
    try – Attempts to execute code that might raise an exception.
    while – Starts a loop that runs while a condition is true.
    with – Manages contextual resources (like files) safely.
    yield – Returns a value from a generator, pausing execution.

""" 

# ---------